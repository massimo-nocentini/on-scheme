
os: osx

language: scheme

before_install:
    - brew update && brew install chicken
    #- sudo apt-get install -y chicken-bin
    - chicken-install test matchable numbers datatype

script:
    - cd src && make all 
